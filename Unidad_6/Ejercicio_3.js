/**
 *  * Ejemplo
 */
const PI = 3.1416;
console.log(`El valor de PI es: ${PI}`);

/**
 *  * Ejercicio
 */

const seg_min = 60;
const min_horas = 60;
const horas_dia = 24;
console.log(
  `Un minuto tiene: ${seg_min} segundos, una hora tiene ${min_horas} minutos y un dia tiene ${horas_dia} horas`
);
